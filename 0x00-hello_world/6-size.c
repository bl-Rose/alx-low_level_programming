#include <stdio.h>
/**
 * main - a computer that prints the size of various types on the computer it is compiled and run on
 * Return 0(success)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float f;

printf("Size of a char: %lu bytes(s)\n", (unsigned long)sizeof(a);
